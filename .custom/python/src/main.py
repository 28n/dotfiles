# Cli interactivity and spin up / ensure workers, then start healthcheck polls

import sys
import ArgumentParser from argparse

def main():
    args = sys.argv
    parser = ArgumentParser(
            prog="shell",
            description="shell utils",
            epilog="glhf")

    parser.add_argument

    

if __name__ == "__main__":
    main()
